# larpix-testing-db-interface

This repo contains example code for interfacing with the larpix-testing-db via the REST api

# Installation instructions
To install run
```
pip install -r requirements.txt
```
Then in a interactive python session, you may run
```
from interface import *
```
